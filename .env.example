# Optional: only needed if an AI-backed enricher is enabled later.
# The system runs fully deterministically without this key.
# ANTHROPIC_API_KEY=

# Optional: only needed for Arcade integration (Phase 3).
# ARCADE_API_KEY=
# ARCADE_GATEWAY_URL=
